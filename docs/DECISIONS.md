# Jobcu decision log

Every decision made while building Jobcu, with a short reason, newest at the bottom. Decisions
marked **Decided** in the [original concept](HANDOVER.md) come from the owner and aren't repeated
here unless something about them was clarified.

**How to read it:** the newest row on a topic wins. A row that a later decision replaced starts
with *Superseded (date)* and says where to look; it stays for the record. When you add a decision
that replaces an older one, mark the older row the same way in the same commit.

---

## 2026-09-17: Phase 0, Foundations

### Confirmed with the owner

| Decision | Reason |
|---|---|
| The copyright holder's name is spelled **Utku Deniz Altiok** | Confirmed by the owner before the first commit, as the concept document requires. |
| The helper tool **uv** is installed on the owner's Mac through Homebrew | The owner agreed. Homebrew was already on the Mac, so this was the simplest route. |
| The code lives in a **private GitHub repository called `jobcu`** on the owner's account | The owner agreed. |

### Technology

| Decision | Reason |
|---|---|
| **Python 3.13** for the app's engine | A proven version that all the building blocks we need support on macOS and Windows. |
| **uv** installs Python and the building blocks, and starts the app | One small tool does everything on both macOS and Windows, so nobody has to install Python by hand. Exact versions are locked in `uv.lock`, so every computer gets the same setup. |
| **FastAPI** with **Uvicorn** for the local web server | Widely used and well maintained. It suits Jobcu's screen talking to its engine, can show search progress live, and checks the AI's structured answers. |
| **Plain HTML, CSS and JavaScript** for the screen, with no build tools | No extra tools to install or keep updated, and the files can be read and fixed directly. |
| **System fonts only; the screen never loads files from the internet** | The concept allows connections only to job sources and the AI provider. A security rule in the page enforces this, and a test checks it. |
| **SQLite** will store job states (Saved, Applied, Not interested, already seen) | Built into Python, a single file in the data folder, and needs no setup. The database arrives in Phase 1, when there is something to store. |

### Where data lives

| Decision | Reason |
|---|---|
| Data folder on macOS: `~/Library/Application Support/Jobcu` | The standard place for app data on a Mac. It's outside the code folder, so updates never touch it. |
| Data folder on Windows: `C:\Users\<name>\AppData\Local\Jobcu` | The standard place for app data on Windows. "Local" (not "Roaming") keeps it on that one computer. |
| The data folder holds a short `README.txt` explaining what it is | So anyone who finds the folder knows not to share or delete it by accident. |
| **API keys are stored in `keys.json` inside the data folder**, readable only by the user's own account, instead of in the Mac Keychain or Windows Credential Manager | The concept allows either. The Keychain can show confusing "python wants to access your keychain" pop-ups after updates, and it behaves differently on Windows. A private file in the data folder works the same everywhere. Keys are never logged and are only shown masked (`••••3f9a`). |

### Safety

| Decision | Reason |
|---|---|
| Jobcu's server only accepts connections **from the same computer** (127.0.0.1) | Nobody else on the same Wi-Fi can open it. |
| The server **refuses requests addressed to other website names**, and anything that changes data must carry a Jobcu-only header from Jobcu's own page | Stops other websites open in the same browser from using Jobcu, or your AI key, behind your back. |
| A **safety check** (`tools/check_no_secrets.py`) blocks API keys, CV-type documents (PDF, Word, etc.), databases and key files | Required by the concept. It runs before every commit on the owner's Mac (a Git "hook", an automatic step Git runs first) and again on GitHub after every upload. Fake test documents are allowed only in `tests/fixtures/fake_documents/`, named `fake_*`. |
| `.gitignore` also keeps these files out, from the first commit on | A second layer of protection, so the files are never even offered for upload. |

### Starting the app

| Decision | Reason |
|---|---|
| Double-click **`Start Jobcu.command`** (Mac) or **`Start Jobcu.bat`** (Windows) | The simplest one-action start on each system. |
| A small text window stays open while Jobcu runs; **closing it stops Jobcu** | Easy to understand, and messages stay readable if something goes wrong. A more polished start can come in Phase 4. |
| Jobcu always tries the same address, `http://127.0.0.1:8765`, and uses another free one only if that's taken | The address stays the same, so a bookmark keeps working. |
| Double-clicking Start while Jobcu is already running just opens it in the browser | Prevents two copies running at once. |

### Testing and GitHub

| Decision | Reason |
|---|---|
| **Automatic tests on GitHub on both macOS and Windows** after every upload, including starting Jobcu with the real launcher files | The concept requires Windows support from the start. |
| Tests always use a throwaway data folder | Tests can never touch anyone's real data or keys. |
| The GitHub tests use the account's **free monthly allowance of test minutes** | No cost. Without a payment method on file, GitHub simply pauses the tests when the allowance runs out. |
| The repository starts on the owner's personal GitHub account. **Before friends are invited (Phase 4), it moves to a free GitHub "organization"** (a shared space on GitHub) | On a personal account, anyone given access can also change the code. An organization lets the owner choose per friend: download-only (the original plan) or allowed to propose changes that the owner approves. The owner mentioned that friends may contribute later, so this choice is made with him in Phase 4. Moving keeps all history and dates. |
| The concept document is kept unchanged at `docs/HANDOVER.md` from the first commit | The concept requires this as a dated record of the owner's work. |

### The owner's own AI provider

| Decision | Reason |
|---|---|
| For **his own copy** of Jobcu, the owner chose **Google (Gemini)** after a neutral comparison | His choice. It only affects his settings: Jobcu itself still recommends no provider and has no default, and every user picks their own. |
| *Superseded (2026-09-22): his Gemini use is now paid, see "The owner's Google billing".* He starts on Google's **free allowance, with no billing set up yet** | Nothing is paid for before real usage is measured in Phase 1. If paid use turns out to be needed, the owner is asked first and shown how to set a monthly spending cap. |
| The owner's Adzuna, Reed and Gemini keys are kept in Apple's Passwords app until Jobcu's settings screen exists | Keys are never pasted into chat or saved in the code folder. |

### Backups and sharing

| Decision | Reason |
|---|---|
| **No separate backup copies. GitHub is the backup.** This replaces the backup suggestion in HANDOVER section 14.1. A backup tool was built, tested, then removed on the owner's request. | The owner's decision: GitHub already keeps the full dated history. |
| **Everything that isn't private goes to GitHub:** code, the concept document, the decision log, progress, setup and contributor instructions, and the guides | The owner wants the repository to be complete and up to date, so friends can understand the project and possibly contribute later. Private things (keys, CVs, personal data, the owner's own settings) never go there, and the safety check enforces this. |
| **Friends may improve Jobcu, possibly with their own AI coding assistants.** The repository stays private: invited friends only, never public. | The owner's decision (2026-09-17). It updates HANDOVER 14.1, which planned download-only access. Friends still can't change the main code directly: they propose changes as pull requests, and the owner approves them. |
| **`AGENTS.md` holds the instructions for all AI coding assistants.** `CLAUDE.md` just imports it, `.gemini/settings.json` points Gemini CLI to it, and `.aider.conf.yml` points Aider to it. | AGENTS.md is the shared standard read by most AI coding tools (Codex, Cursor, Copilot, Gemini CLI, Windsurf, Junie, Aider and more). One file means no copies drifting apart, whichever tool a friend uses. |
| **`CONTRIBUTING.md` explains setup and sending changes in plain words**, for Mac and Windows | Friends may not be programmers. |
| Friends contribute from **their own private copy (a fork) through pull requests** | Branch protection isn't available for private repositories on free GitHub plans. Forks with pull requests keep the owner's main code and its history safe at no cost. |
| `CONTRIBUTING.md` says contributions become part of Jobcu under its LICENSE | Keeps Jobcu clearly the owner's work, in friendly wording with nothing to sign. |
| GitHub forms for pull requests, problem reports and ideas | Helps non-programmers describe changes and problems clearly, and reminds everyone not to share keys or personal data. |
| **`.editorconfig`** sets shared text settings (UTF-8, line endings, indentation) | Understood by most editors and AI tools, so files stay consistent on Mac and Windows. |
| The keys guide (`docs/guides/getting-your-keys.md`) is written now, from the steps the owner actually followed, with neutral instructions for several AI providers | Keeps the verified steps instead of losing them in a chat. Screenshots follow in Phase 4. |
| A test checks that links between the project's documents still work | Friends and AI tools will edit the docs, and broken links would confuse readers. |

---

## 2026-09-17: Phase 1, Usable first version

### Confirmed with the owner

| Decision | Reason |
|---|---|
| **Job sources: clearly permitted sources first.** Official job APIs, government job agencies, company career systems and AI web search are built. Reading LinkedIn, Indeed, StepStone, Glassdoor etc. pages directly is **on hold** until the coverage test (Phase 3) shows how many jobs would really be missed. | The owner wants to avoid legal risk. This updates HANDOVER 9.4, which had accepted that grey area. |
| **Score check: "quick review".** Claude collects about 40 real job ads and pre-fills a rating for each; the owner only corrects the ones he disagrees with. | The AI still scores every job automatically in every search. This one-time answer key checks that the scores match the owner's own judgement, with less work for him than rating everything. |
| **Cover letters may be written for one specific application.** When reading a cover letter, Jobcu ignores everything specific to that one application (the company, that job title, that company's country or city, plans to move there) and keeps only what's generally true about the person and what they want. **Where to work comes only from the location box.** | The owner's instruction. HANDOVER assumes a generic cover letter, but real ones are often tailored. |

### Technology

| Decision | Reason |
|---|---|
| Each native AI provider is reached through **its official Python library** (`google-genai`, `openai`, `anthropic`), wrapped in Jobcu's own AI layer. "Other (OpenAI-compatible)" providers use the `openai` library with the provider's address. | The providers maintain these libraries and keep them current, which matters because AI APIs change often. Jobcu's layer makes every provider behave the same for the rest of the app. |
| The libraries' own automatic retries are switched off; **Jobcu's AI layer retries** instead | Waiting and the plain messages ("AI limit reached, continuing more slowly") then work the same for every provider, and no progress is lost. |
| OpenAI requests use `store=False` | The concept keeps user data private; this asks OpenAI not to keep the conversation for later use. |
| **No copyleft libraries** (GPL, AGPL and similar) | Jobcu is "all rights reserved", and those licences could require publishing its code. |

### AI settings and keys

| Decision | Reason |
|---|---|
| Providers are shown **alphabetically**, with "Other (OpenAI-compatible)" last, and **none is pre-selected** | Neutrality: Jobcu never suggests a provider. |
| Each provider's key is saved separately (`ai_anthropic`, `ai_gemini`, …) | Switching provider doesn't delete the other keys, so switching back is easy. |
| Model names come from the provider's own model list (the "Load model list" button) or are typed in; **none is built into Jobcu** | Providers rename and retire models often (HANDOVER §13). |
| Reasoning effort starts at "low" for scoring and "medium" for reading documents and the location; models that don't support a setting are retried without it automatically | HANDOVER §13 asks for the lowest setting that keeps quality. These starting values get checked against the quality test set. |
| The setup check asks the model for a tiny answer in Jobcu's JSON format, and reports rate limits instead of waiting | The check has to be quick and prove that structured answers work, not just that the key is valid. |
| Saved keys are shown only as their last four characters | Enough to recognise a key without revealing it. |
| The log file hides anything that looks like a key, and the Adzuna check never logs its request address (which contains the key) | A safety net against keys ending up in log files. |

### CV, cover letter and profile

| Decision | Reason |
|---|---|
| Documents are read with **pypdf** (PDF) and **python-docx** (Word), both permissive licences | Well maintained and install on Mac and Windows without extra tools. |
| **PDFs are read two ways** (plain and layout-aware) and the cleaner result is kept | Different PDF programs store text very differently. The owner's own CV reads best one way and his cover letter the other. |
| A file is **read when it's uploaded**, so problems (scanned image, password, damaged file) show up right away with advice | Better than finding out in the middle of a search. |
| **Text is never cut.** Documents longer than 60,000 characters are refused with a message | Silently cutting a document could leave out important details. No real CV or cover letter is that long. |
| Uploaded documents are stored in the data folder, readable only by the user | They're personal data. |
| The AI reads the documents with the **"careful" model and reasoning setting** (the optional second model) | HANDOVER §13 names reading the CV and cover letter as a reasoning-heavy step. |
| The profile lists **what was left out because it was about one specific application** | People can see that company names, job titles and countries from a tailored cover letter weren't used. |
| The profile keeps languages **as written**, plus a CEFR level marked "estimated" when it comes from words like "fluent" | Honest about what's stated and what's guessed. It helps scoring without claiming certainty. |
| Document text is marked as data in the prompt, and the AI is told to ignore instructions inside documents | Protects against text in a document that tries to steer the AI. |
| "What Jobcu understood" can be opened before any search, and **nothing is saved** | People can check the AI understood them before searching. HANDOVER §4 says the profile isn't kept. |

### Real tests with the owner's keys

| Decision | Reason |
|---|---|
| **The owner allowed Claude to use his keys for development** (2026-09-17). Claude uses the keys already saved in Jobcu on his Mac, only through Jobcu's own code, and never prints, logs, copies or commits them. | Real tests with the real AI and real job sources give much better development than made-up data. Keys never have to appear in the chat: it's stored outside the owner's computer and adds no benefit, because Claude already works on that computer. |
| *Superseded in part (2026-09-22): his Gemini use is now paid, see "The owner's Google billing".* Development tests use **free usage only**. Claude asks before anything that could cost money, and keeps Adzuna and Reed requests modest. | The owner's Gemini account has no billing set up, and the job-site keys have daily limits. |
| **Experience is split into full-time years and internship / working-student / part-time years** | The first real test counted the owner's part-time student jobs as "3.1 years of experience". Job ads asking for "3+ years" usually mean full-time work, so that would have overstated seniority fit. |

### Location, search words and running a search

| Decision | Reason |
|---|---|
| **A newer Jobcu automatically replaces an older copy that's still running** (the code has a fingerprint; the new copy asks the old one to stop) | The owner double-clicked Start Jobcu after an update and still saw the old screens, because the old copy was running. |
| *Superseded (2026-09-17 night): conditions are researched, see "The location box is a research task".* Phase 1 location understanding handles **named countries, regions and places**. Any other condition is shown as "Not checked yet" until the smart filter in Phase 2. An empty box means all 31 countries, with a note that this takes longer. | Matches the phased plan (HANDOVER §16). Nothing is silently ignored. |
| *Superseded (2026-09-21 night): facts can now narrow the countries searched, see "The owner's answers on travel and on facts".* The AI doesn't guess countries for descriptions like "by the seaside" | Guesses about places belong in Phase 2, where they'll be checked against real data or clearly labelled "AI estimate". |
| **Search words:** up to 16 job titles and 8 field or skill words per language, in English plus the job-ad languages of the countries searched (a small table in `countries.py`). At most 5 languages per AI request. | Covers the common title variants employers use (HANDOVER §5) while keeping requests reliable and affordable. |
| Search progress is shown by the screen **asking Jobcu once a second** | Simpler and sturdier than a live connection, and plenty fast for a search that takes minutes. |
| **One search at a time** | Keeps usage predictable and avoids two searches competing for the same free AI allowance. |
| The search form (location text and filters) is **remembered for the next visit** | Convenience only. Every search still reads the documents and the location text again from scratch. |
| After a search, "What Jobcu understood" shows **exactly what that search used**. Before any search, it reads the documents on the spot. | The user sees what the search really worked with. |

### Owner's choices for the smart location filter (Phase 2 design, decided 2026-09-17)

Based on the owner's own test sentence: *"Germany or Ireland or UK, no AfD or other fascist supporter
cities (dominant election result). Also the job location should be at most 50 minutes to a city that
has at least 0.3% of its country's total population."*

| Decision | Reason |
|---|---|
| **The AI settles unclear wording itself** instead of asking the user, and shows exactly how it read each condition under "Understood as", where the user can edit it | The owner's principle: interpreting fuzzy wording is why Jobcu uses an AI. It also matches HANDOVER §6 (no confirmation step before searching). |
| **Political conditions:** when the user describes parties by a label (e.g. "far-right" or "fascist supporter") instead of naming them, the AI researches, with live web search, which parties reliable current sources put in that group for each country. It lists the parties it used, with source links, in "Understood as". | The owner asked the AI to research and decide. Showing the parties and sources keeps the choice transparent and correctable. Jobcu itself takes no political position; it applies the user's own condition. |
| **"Dominant election result"** is read as **above the party's national average** in the latest national parliamentary election, unless the user's wording says otherwise. Results come from official sources for each place, so failing places are hidden (verified). | The owner's reading, e.g. Dresden should be excluded. |
| **Travel time** uses the travel mode the user writes. **If none is given, it means public transport.** | The owner's choice. Exact public-transport times need a route-planning source; Phase 2 will look for a free, permitted one and otherwise show a clearly labelled estimate. |
| *Superseded in part (2026-09-17 evening): Maps may use part of the €30 budget, see "Owner's guidance on usage and money".* **Google Maps is the preferred source for travel times**, used **only within Google's free monthly allowance**. The owner doesn't want to pay for it. It stays an **optional key**: without it, Jobcu shows a clearly labelled estimate. | The owner's choice (2026-09-17). Google has the most complete public-transport timetables. Keeping it optional keeps setup simple for friends. |
| Travel time is **one simple number per place**: from the job's town to the **centre** of the nearest big-enough city, by the mode the user writes (default public transport), for a normal weekday trip during working hours. Jobcu asks Google **once per place, not per job**, never for places already inside a big-enough city, never for places obviously too far away, and remembers answers for as long as Google's terms allow. | Keeps usage far below the free allowance: estimated at a few hundred to about 1,500 requests a month, even with 5 searches a day. |
| **Two safety stops against charges:** Jobcu counts its own Google requests and stops well below the free allowance each month, and the owner sets a daily limit in Google's own settings so Google itself refuses anything beyond it. Google needs a payment card on file even for free use; the owner approves before setting that up. Exact numbers and terms are confirmed in Phase 2. | The owner must never be charged for Google Maps. |
| City size given as a share of the country's population is computed per country from official population figures (0.3% ≈ 250,000 in Germany, ≈ 200,000 in the UK, ≈ 16,000 in Ireland) | Follows the user's rule exactly. The very different results per country are shown in "Understood as". |

### Job sources, duplicates, filters and scoring (Phase 1, part 4)

| Decision | Reason |
|---|---|
| **Adzuna** is queried economically: single-word search words are combined into one "any of these words" request, multi-word phrases need a request each (field words first), results come newest first and paging stops at the first job older than the window. Jobcu stops at **40 requests per search, 240 a day and 2,400 a month**, and each country or place gets a fair share. | Adzuna's free key allows 250 requests a day and 2,500 a month. Real tests showed boolean queries like `"a" OR "b"` return nothing. |
| **Reed:** one request per English search word, reading all pages (Reed can't sort by date or combine words); jobs proven older than the window are skipped. | Tested: Reed ignores OR and returns results in no date order. |
| **Bundesagentur für Arbeit:** one request per German or English search word, using the "published since" day filter; the **first publication date** is used, and each job's real country is read from its address. | The first publication date catches reposted old jobs. A real test showed the agency also lists jobs in Austria. |
| **Short versions first, full ads later:** sources return short versions; full ads are fetched only for jobs that survive duplicates, the rules filter and the quick relevance check. | In a real test, fetching every full ad used 250 Reed requests for one week of UK jobs; most were irrelevant. |
| *Superseded (2026-09-24): Adzuna's pages aren't read, see "What to do with Adzuna".* **Adzuna's job pages are read for the full ad** (the owner's decision, 2026-09-17): only for jobs that passed the quick relevance check, reading the page a person sees when clicking the job, with a 3-second pause between pages. A refused page is skipped and never retried; after 3 refusals in a row, or any "too many requests" or robot-check answer, no more Adzuna pages are read in that search. | The API gives only the first ~500 characters of each ad. The owner accepts this grey area for better scores, but never working around a block. A real test showed why it matters: full ads changed scores a lot (a PCB designer job went from 87 to 68 because the full ad asks for good German and a vocational background; another from 96 to 87 because it asks for 5+ years). |
| *Superseded (2026-09-24): Adzuna's pages aren't read.* Adzuna sometimes refuses a **single** ad's page ("Zugriff verweigert") while other pages load normally, so one refusal doesn't stop page reading | Found in a real search: stopping after the first refusal left 4 of 22 jobs with only summaries. |
| Job pages are read through a **general reader for schema.org JobPosting data** (`jobposting.py`), the standard job data most job boards and career sites include for search engines | It gives the full text, posting date, job type and remote status in one format across thousands of sites, and Phase 3 reuses it for company career pages. |
| When a source leads to the **employer's own page**, that page becomes the job's main link, with the source listed under "Also on". Staffing agency ads are excluded. | HANDOVER §10 puts the employer's own page first. An agency's page isn't the employer's. |
| *Superseded (2026-09-17 night): career systems are read from Phase 1 on, see "Company career systems".* **Company career systems** (Greenhouse, Personio, Workday and others) move to **Phase 3**, together with the employer directory | In 815 real job ads, the employer links from Reed and the job agency pointed to company homepages, staffing agencies and other job boards, never to a career system. Company discovery through links would find almost nothing. |
| Posting dates known **only by day** are hidden only when the whole day lies before the window, and are shown as "posted today" or "posted yesterday" | Such a job can't be proven too old, so it isn't hidden (HANDOVER §7). |
| **Duplicate rules:** copies of the same company and place match when their titles match word by word (typos allowed); titles at a different level ("Senior", "Werkstudent") or with different reference numbers never match; **staffing agency** ads need matching full text to merge, and an agency ad that repeats an employer's own ad is tagged "possible duplicate" | Tested on 588 real ads. Looser fuzzy matching wrongly merged "Lead Electronics Engineer" with "Electronics Engineer", and "Elektronik" with "Elektrotechnik". Agencies (Ferchau, Brunel, Akkodis and others) post near-identical ads for different clients. |
| **Quick relevance check is on** (title, company and the first lines, 40 jobs per AI request). Only clearly unrelated jobs are left out, and their titles are listed in Search details. **Provisional until the quality test set confirms it.** | Without it, a single search can have 300+ jobs to score, which would use up a free AI allowance quickly. HANDOVER §13 requires checking it against the test set, which comes next. |
| *Superseded in part (2026-09-23 evening): the language part and the limits for blockers are worked out in code, see "What the owner's third search showed".* **Scoring:** 4 jobs per AI request, each rubric part scored separately and **added up in code**; a job the AI skips is asked about again on its own. Batch size is provisional until the quality test. | The total always matches the parts, and no job silently goes unscored. |
| When the scoring limit is reached, the search **asks** before scoring more. Jobs not scored are **still shown** (without a score). | HANDOVER §13: caps never silently reduce coverage. |
| The last search's results are saved and shown again after Jobcu restarts. Saved and Applied lists include jobs from all searches. "Show hidden" brings back jobs marked Not interested. | HANDOVER §12. |
| *Superseded (2026-09-17 night and 2026-09-21): the town list and travel limits measure places, see those sections.* In Phase 1, a job's country is labelled **verified** when the source states it. Places and distances rely on each source's own location filter until Phase 2. | Honest labelling until the smart location filter arrives. |

---

## 2026-09-17 (evening): Owner's guidance on usage and money

| Decision | Reason |
|---|---|
| *Superseded (2026-09-24): one 24-hour search a day, see "Medium reasoning everywhere".* **Expected use: usually 3 searches a day, at most 5.** Limits should fit this real use and must not be so strict that results get worse. | The owner's guidance. Earlier planning assumed 5 a day as the norm. |
| *Superseded in part (2026-09-24): the number of searches a day is measured, see "What to do with Adzuna".* **Adzuna's request budget per search will adapt** to the remaining free monthly and daily allowance, assuming about 3 searches a day, instead of a fixed 40 per search | 2,500 requests a month ÷ about 90 searches ≈ 27 per search on average. A fixed 40 would run out after about 60 searches, while light days leave allowance unused. |
| *Superseded (2026-09-24): €25 for the owner, free or under €10 for others, see "Cloud sessions, a universal Jobcu".* **Money:** up to **€30 a month in total** for everything (AI first, other services if they clearly help). Free is best and cheaper is better; paying is fine only where it really improves results. Before anything that costs money is set up, the owner is told the expected cost. | The owner's guidance. This replaces the earlier "Google Maps only within its free allowance" rule: Maps may use part of the €30 if it clearly helps, though the owner expects the free allowance to be enough. |
| **Reading full job pages is best effort.** If a site doesn't allow it, Jobcu scores from what it has and carries on; this is never a reason to stop a search. | The owner's guidance: full ads help, but a platform refusing them isn't critical. |

## Working across sessions

| Decision | Reason |
|---|---|
| *Superseded (2026-09-22): see "Keeping the project recoverable and tidy".* `docs/PROGRESS.md` starts with a **"Right now"** section (where the project stands, what waits on the owner, the next tasks in order). Every AI session reads it first and updates it after each finished step and before the conversation is cleared. | The owner clears long conversations (`/clear`). Everything needed to continue must live in the project, not in one chat. |
| Hard-won facts about each job source live in **`docs/SOURCES.md`** | Source behaviour (limits, quirks, formats) took real testing to learn and is needed again for Phase 3. |

## 2026-09-17 (evening): First outside tester

| Decision | Reason |
|---|---|
| **The Start Jobcu files install the helper program uv themselves**, after asking (Mac: press Return; Windows: press any key), using uv's official installer from astral.sh into the user's own folder, without changing shell settings. GitHub tests the "helper missing" path on both systems. | The owner is sharing Jobcu with a non-technical friend. Before this, a missing uv meant a dead end. This was planned for Phase 4 but is needed now. |
| **Tester-ready documents now, ahead of Phase 4:** the README first screen for testers, and guides for installing and starting (Mac and Windows, including the Apple "could not verify" and Windows "protected your PC" warnings), the first search, and troubleshooting. Pictures come later. | Someone who has never used GitHub must be able to install and use Jobcu from the repository alone (HANDOVER §2.1). Real tester feedback helps the scoring work. |
| **A tester gets access by being invited to the private repository as a collaborator.** Before more friends are invited, the repository moves to a free organization, as planned. | For one trusted friend this is the simplest path. Collaborators on a personal repository can technically change files, but every change stays visible in the history and can be undone. |
| The GitHub "Download ZIP" keeps the Mac start file runnable (checked: `jobcu-main/Start Jobcu.command` keeps its "can be started" setting) | Otherwise the Mac guide wouldn't work. |

## 2026-09-17 (late evening): Documentation style and country priority

| Decision | Reason |
|---|---|
| **Two audiences, kept apart.** Everyday users get `README.md` and `docs/guides/`: short, simple, no technical words, just enough to use Jobcu fully. Developers get `CONTRIBUTING.md` (developer guide), `AGENTS.md` and the files in `docs/`: technical and detailed. | The owner's instruction: the user side must be clear and sleek, not bloated. Technical detail belongs to developers. |
| *Superseded (2026-09-24): see "The order of countries".* **Top-priority countries: Ireland, the UK and Germany.** Other EU and European countries are also important, but these three come first for coverage work and testing. | The owner's instruction. |
| **Ireland wasn't connected yet** because the Phase 1 starter sources (Adzuna, Reed, Bundesagentur) came from the original plan, and Adzuna turned out not to cover Ireland. **Connecting Irish sources is now the next task**, ahead of the rest of Phase 1, and coverage for the UK and Germany is strengthened at the same time. Clearly permitted sources come first (owner's earlier choice). | Coverage is Jobcu's main purpose (HANDOVER §9.0), and Ireland is a top-priority country with no source at all today. |
| **Priority means order, not exclusion.** The goal stays **maximum coverage across every supported European country**: as many platforms, regions and ways of finding jobs as possible, so that whatever places a user prefers, Jobcu covers them well. Ireland, the UK and Germany are simply worked on and tested first. | The owner's clarification (2026-09-17). Matches HANDOVER §9.0: coverage is Jobcu's main purpose. |
| **Don't rely only on sites that offer an API.** Jobcu should reach jobs anywhere on the web, **without legal risk**, through routes like these: official APIs and open data; public employment services of each country and EURES; job feeds that sites publish for sharing (XML/RSS); company career systems' public job lists; the standard job data (schema.org JobPosting) that career sites and boards publish for search engines; an employer directory per country; and the AI provider's live web search to discover job pages anywhere. Always: no logins, respect robots.txt and site terms, never work around blocks, polite pace. | The owner's instruction. Each route is verified first and recorded in `docs/SOURCES.md`. Job boards whose terms forbid automated reading stay on hold until the coverage numbers are in (owner's earlier choice). |

## 2026-09-17 (night): Countries, reusing AI work, optimising requests

| Decision | Reason |
|---|---|
| *Superseded (2026-09-17 night): 30 countries, see below.* **Supported countries: every country that lies fully or mostly in Europe** (45 in total), replacing "EU + UK, Switzerland, Norway, Iceland" (HANDOVER §1, §17). Added: Albania, Andorra, Belarus, Bosnia and Herzegovina, Kosovo, Liechtenstein, Moldova, Monaco, Montenegro, North Macedonia, San Marino, Serbia, Ukraine, Vatican City. Not included: countries mostly in Asia (Turkey, Russia, Kazakhstan, Georgia, Armenia, Azerbaijan). Cyprus stays as an EU member. Job-ad languages for each are in `countries.py`. | The owner's decision. The goal is coverage across the European continent. |
| **Reusing AI work: only for understanding the person.** When the CV text and cover letter text are exactly the same as in an earlier search (and the profile prompt and model are unchanged), Jobcu reuses the earlier profile. **Everything about the job search stays fresh every search:** location understanding, search words, finding jobs, the quick relevance check and scores. | The owner's decision. It saves AI allowance without any effect on finding the freshest, best jobs. HANDOVER §4/§12 ("profile not kept") is updated for identical documents only. |
| **Request optimisation must never cost quality: never fewer fresh jobs, never worse scores.** Allowed savings: adaptive per-search budgets for sources with free limits; results that come newest first are paged only until older than the window; full ads are fetched only for jobs still in the running; within a search nothing is fetched twice; the job **ad text** (not scores) may be remembered by job ID for a few days, so the same ad isn't downloaded again; AI requests are batched and use provider-side prompt caching where available. | The owner's instruction: "the best of both worlds". Each saving is measured in Search details. |

| *Superseded (2026-09-17 night): 30 countries, see the next row.* **Supported countries changed to a top 20** (this overrides the 45-country decision above): the 20 European countries with the highest GDP per person (IMF World Economic Outlook 2025, current US dollars), leaving out tiny states under 100,000 people. **Austria, Belgium, Cyprus, Czechia, Denmark, Finland, France, Germany, Iceland, Ireland, Italy, Luxembourg, Malta, Netherlands, Norway, Slovenia, Spain, Sweden, Switzerland, United Kingdom.** Ireland, the UK and Germany are still worked on first; coverage within these 20 should be as complete as possible. Compared with the original list (HANDOVER §1), it no longer includes the EU countries outside the top 20 (e.g. Poland, Portugal, Greece). | The owner's decision: 45 countries would bloat the app and spread the work too thin. Tiny states (Liechtenstein, San Marino, Andorra, Monaco, Vatican City) rank high but have very few jobs. |
| **Supported countries: 30** (this overrides the top-20 decision above): the top 20 plus **Poland, Portugal, Romania, Greece, Hungary, Croatia, Slovakia, Estonia, Latvia and Lithuania**. Still left out: tiny states, and Bulgaria, Serbia and Ukraine among the larger ones. | The owner's decision after reviewing which large and well-known countries the top 20 left out. |
| **Search-word languages follow the location only:** English plus the job-ad languages of the countries searched. When places are named in a country with several languages, only those places' languages are used (Zürich: German; Geneva: French; Brussels: French and Dutch). An empty location still needs every language. | The owner's instruction: no unnecessary translation. Fewer languages means fewer AI tokens and fewer source requests, with no jobs lost, because ads in a place are written in its languages or English. Checked live: "Zürich or Geneva" → English, German, French. |

## 2026-09-17 (night): Coverage work for Ireland, the UK and Germany

| Decision | Reason |
|---|---|
| **JobsIreland.ie is a source** (Ireland's public employment service). Jobcu reads its newest-first job list page by page until jobs are older than "Posted within", matches titles to the search words itself, and reads a job's own page for the full ad only when the job is still in the running. | Ireland had no source at all. There's no API, but robots.txt allows everything and the site's terms say the information is meant for jobseekers searching for work; Jobcu shows it only to the person searching and never re-publishes it. Its own keyword search looks at titles only, so reading the list is the same result with fewer requests. |
| **Sources that can only list jobs are matched on Jobcu's side** (`sources/matching.py`): a job title must contain every word of a job-title search word (in any order, also inside longer words), field words may also match the ad text, and named places must appear in the job's location. | Generous matching keeps coverage high; the quick relevance check and scoring still decide relevance. Distances ("within 50 km") need map data, which comes with Phase 2; until then a nearby town is only kept when its address names the place (Irish addresses name the county). |
| **EURES is not used**, although its search API works well | Its "Find a job" terms forbid automated extraction of vacancy data for further processing and allow API use only for EURES partner organisations. The owner wants no legal risk. |
| **UK Find a Job (DWP) is not used** | It refuses Jobcu's requests with a web-application firewall. Jobcu never works around bot protection. |
| **Community Employment scheme jobs count as part-time, apprenticeships and work placements as "internship or working student", self-employed as "freelance or contract"** | Closest matches among Jobcu's job types, the same mapping style as the Bundesagentur's. |

## 2026-09-17 (night): Company career systems, the employer directory and Arbeitnow

| Decision | Reason |
|---|---|
| **Company career systems are read from Phase 1 on** (Greenhouse, Lever, Ashby, Workable, Recruitee and Workday), through the public job lists those systems publish for career pages. This brings HANDOVER §9.3 forward from Phase 3. | Ireland's engineering jobs are mostly posted on companies' own career sites first, and Adzuna doesn't cover Ireland at all. Career-system ads are the original, earliest and most accurate version of a job. |
| **An employer directory ships with Jobcu** (`src/jobcu/data/employers.json`): the companies Jobcu knows, their career system and the supported countries they hire in. It's checked and refreshed with `tools/check_employers.py` (one request per company), from public career sites only, **never from anyone's searches**. | HANDOVER §9.3: career systems can't be searched across companies, so Jobcu needs a list. Keeping it general reference data keeps every search fresh and private. |
| **Only companies with jobs in the supported countries stay in the directory**, and each entry records whether the company also hires elsewhere. | Keeps searches short (no requests for companies that don't hire here) and lets Jobcu judge unclear locations: at a company that only hires in Ireland, "Head Office" is Ireland. |
| **A free-text location is turned into a country by `placenames.py`** (country names in English and local languages, regions, and the bigger towns, plus well-known places elsewhere). | Career systems give locations only as text. It also protects against "Dublin, CA" or "Cambridge, MA" being taken for the Irish or English ones. Real map data comes with the smart location filter in Phase 2. |
| **SmartRecruiters is not read** although its Posting API is public | Its robots.txt allows only LinkedIn's crawler. Jobcu respects robots.txt. |
| **Workday sites are read only after checking each site's robots.txt** | Workday's job list address isn't documented, so Jobcu asks each company's own site whether automated reading is allowed, and skips it if not. |
| **Arbeitnow is a source** (free public API, no key) | It carries jobs from career systems of hundreds of German and British companies that aren't in the directory, with full ad texts and exact posting times. Its terms allow free use and ask for a link back, which Jobcu's job link gives. |
| Companies whose own servers differ per company (Workday, Recruitee) are read **four at a time**; the polite pace per server is unchanged. | A search reads many companies; without this, big directories would make searches take much longer. Shared servers (Greenhouse, Lever, Ashby, Workable) are still read one request at a time. |

| **Jobcu ships a list of towns with coordinates** (`src/jobcu/data/places.csv.gz`, about 900 KB: every town with at least 1,000 inhabitants in the 30 countries, with its names in the local languages, from GeoNames, CC BY 4.0, rebuilt with `tools/update_places.py`). Sources that give only a place name are now matched by **real distance**, so "Munich or within 50 km" finds a career-site job in Garching. Without a distance, a named city counts as 25 km around it, the same as the job sites' own filters. | Career sites and Arbeitnow have no location filter, so before this a job was kept only when its text named the place: a real Munich search missed NVIDIA and NXP jobs in and around Munich. GeoNames is free, permissively licensed (credited in README.md) and also gives the population figures Phase 2 needs for "a city with at least 0.3% of the country's people". |
| **Jobcu keeps a downloaded full ad for three days** (`ad_texts` in the database), keyed by source and job ID, and reuses it instead of downloading the same ad again. Only the ad text and the facts read with it are kept; scores, the quick check and everything else stay fresh. | The owner's optimisation rule: never fewer fresh jobs, never worse scores. Job ads barely change while they're open, and the same ads come up in several searches a day. |
| **Adzuna's requests per search are worked out from what is left of the free month** (`budget.share_of_month`): what remains this month, divided by the days still to come and about 3 searches a day, kept between 25 and 60 and never above what today's own allowance has left. | The owner's guidance (usually 3 searches a day). A fixed 40 per search ran out after about 60 searches and wasted the allowance on quiet days. |
| **Prompt caching is asked for where a provider needs asking:** Anthropic gets the step's instructions as a cacheable block; OpenAI and Google cache long prompts by themselves. | Scoring sends the same long instructions with every batch of jobs. It lowers cost with no effect on the answers, and Jobcu stays neutral: it's one line inside each provider's adapter. |
| **The score check collects itself from real searches** (`quality.py`, screen "Score check"): each search keeps a few of its ads across the whole score range and a few left-out titles, up to 40 of each, and the owner answers good / okay / poor and whether a left-out title was really unrelated. | HANDOVER §13 asked Claude to collect about 40 ads and pre-fill ratings. Collecting inside Jobcu costs nothing extra (the ads are already in hand), keeps everything on the owner's computer, and grows with his own searches instead of one frozen set. His answers are the answer key for tuning the prompt. |

## 2026-09-17 (night): The location box is a research task for the AI, not a filter

The owner's clarification after reviewing the coverage work. His own examples: *"companies at most
50 minutes from a city centre of a city with at least 0.3% of the country's people"*, *"cities
where far-right voters are below the country's average"*, *"cities with shops open on Sunday"*,
*"cities with several easy-to-reach Turkish supermarkets"*, *"cities where more than 20% are
students"*.

| Decision | Reason |
|---|---|
| **Every search interprets the location text from scratch.** Nothing about it is carried over between searches or between people. Two searches by the same person may mean completely different things. | The owner's instruction. Each person has a different CV, cover letter and idea of where they want to work, and the same person changes it from search to search. |
| **The AI splits the text into conditions and chooses how to check each one:** named places and countries directly; distances and travel times by measuring (Google Maps for real weekday public-transport times, within its free allowance; the shipped town list for straight-line distances); facts about places (elections, shops, students, anything else) by **live web search**, with the sources kept; anything it can't check shown as "not checked". | This is why Jobcu uses an AI at all. The conditions can be anything, so the app must research and verify instead of offering a fixed set of filters. |
| **Whatever the AI researched is shown with its sources**, and estimates are labelled as estimates. The person can correct any condition before it is used. | The owner's principle: the AI decides unclear wording itself, but its working must be visible and correctable. Jobcu takes no position of its own (for example on politics); it applies the person's condition. |
| **Shipped reference data is a ruler, never an interpreter.** The town list (coordinates, population, local names) and any dataset added later only answer "where is this place and how big is it" once the AI has decided what to measure. | Keeps the design open: a search with no place, a whole country, or a condition that has nothing to do with distance never touches it. |
| *Answered (2026-09-21 night and 2026-09-22): only the AI's travel estimates are remembered, 30 days.* **Open question for the owner:** may researched facts that barely change (a town's population, the last election's results) be remembered with their source and date for a while, or must every search research them again? | Re-researching everything makes searches slower and uses more AI. The owner's rule so far is "everything about the job search stays fresh"; this is about background facts, not about jobs. |
| **Jobcu's AI layer can look things up on the web** (`AIClient.research`), through the user's own provider: Anthropic's web search tool, OpenAI's web search tool, or Google's Search grounding. The answer comes back with the pages used, and every look-up counts against the cap in Settings. Providers that can't search say so plainly, and the condition is shown as "not checked". | Needed for the location research the owner described (election results, travel, shops, students) and later for finding jobs anywhere (HANDOVER §9.6). Jobcu itself never contacts a search engine: it stays device-local and provider-neutral. Checked live on the owner's free Google allowance: it answered "AfD in Munich 9.3% vs 20.8% nationally" with sources. |
| **How a location condition is checked** (first version, 2026-09-17): the AI splits the text into conditions; conditions that only depend on **how big a town is** are computed from the figures Jobcu ships (town populations, and people per country in `countries.py`); anything else about a place is **looked up on the web** by the person's own AI provider, which returns the towns that fit or the towns to avoid, plus the pages it used; conditions about the job itself, and anything that couldn't be confirmed, are shown as "not checked" and never filter anything. Jobs whose place can't be recognised are kept, and every job's card says what each condition found. | The owner's examples, checked live on his own free allowance: *"no cities where far-right parties polled above the national average"* → Dresden, Chemnitz, Cottbus, Erfurt, Gera… excluded with sources; *"at least 0.3% of the country's people"* → at least 250,500 in Germany and 15,930 in Ireland, computed, not guessed. Looking up what can be computed would waste look-ups and be less exact. |
| **Web look-ups are capped per search** (30 by default, in Settings), and a condition that runs out of allowance is shown as "not checked" with that reason. | HANDOVER §13 asks for a cap. The first live test hit the old cap of 10 after one condition, which is why the cap is bigger and size conditions no longer use the web at all. |

## 2026-09-21: Correcting how the location was read, after a search

| Decision | Reason |
|---|---|
| **"Edit" next to "Understood as"** (HANDOVER §6, point 2): the person can switch a condition off, correct the towns it found or the town size it worked out, reword it, ask Jobcu to check it again, or add a new one. The changes are applied to **the jobs the search already found**, without searching again. | HANDOVER §6 asks for exactly this, and the 2026-09-17 decision says the person can correct any condition. A new search would take minutes and repeat every web look-up. |
| **Only reworded, new or "check again" conditions use the AI.** Everything else keeps what the search found. Jobs that come back in get the quick relevance check and a score; jobs already scored in that search keep their score, and nothing is asked twice. | The owner's rule: never fewer jobs, never worse scores, no work done twice. The scores come from the same search and the same ads. |
| **A corrected town list or size becomes the person's own rule**: cards say "your change", never "AI estimate". A switched-off condition stays visible, filters nothing and is no longer passed to scoring. After an edit, scoring no longer gets the first "Understood as" sentence, which may mention a switched-off condition. | Every card must say where a verdict came from (HANDOVER §6, point 6). |
| **Jobcu keeps the latest search's jobs** that passed the other rules (dates, job types, remote, countries, Not interested), with their quick-check answers and scores (`pool.py`, table `search_pool`). The next search replaces them, and a new search never reads them. | Corrections must still work after Jobcu restarts. It's the same data as the saved results, on the same computer. It doesn't change "everything starts fresh each search" (HANDOVER §12): it's only used within the search it came from. |
| **Places and countries can't be changed with Edit.** They decide which job sites are asked, so the window says to change "Where do you want to work?" and search again. | Re-applying works only on jobs already found. |

## 2026-09-21: More sources: Sweden's public employment service, Teamtailor, SuccessFactors

| Decision | Reason |
|---|---|
| **Arbetsförmedlingen is a source** for Sweden, through its open JobSearch API (JobTech): every ad in Platsbanken, with full texts. | Sweden had no source of its own. The data is CC0, needs no key, and it's the national public job board (HANDOVER §9.2 names it as a candidate). |
| **Jobcu reads every ad in the "Posted within" window and matches on its own side**, instead of using the API's word search. | The word search misses Swedish compound words and, by default, narrows multi-word titles to one occupation. Coverage comes first (AGENTS.md); a 24-hour search is about 16 requests, and only the fields Jobcu uses are downloaded. |
| **Teamtailor career sites are read** (a seventh career system), through the RSS feed every Teamtailor career site publishes, for the employers in the directory. | Teamtailor is widely used in the UK, Ireland and the Nordics. The feed is public, allowed by robots.txt, and carries full ads with exact times. |
| **SuccessFactors career sites are read** (an eighth career system) through the sitemap each site publishes for search engines, opening a job's own page only when its title matches the search words (for the posting date, place and full ad). | Many large German engineering employers use it (Schaeffler, MTU Aero Engines, ZF, KUKA, SICK, Festo, Endress+Hauser). SuccessFactors' own RSS search is closed by robots.txt, the sitemap and job pages are open. Opening only matching pages keeps requests low. |
| **The employer directory grows to 357 companies** (IE 149, UK 259, DE 232), found through web searches and by probing big employers' career addresses; `check_employers.py --only-new` checks new candidates without re-checking everyone. | Coverage (AGENTS.md). Still general reference data from public career sites, never from anyone's searches. |

## 2026-09-21 (evening): Travel limits to reference places

The owner's correction: a job in Fürstenfeldbruck was left out by *"at most 50 minutes to a city
that has at least 0.3% of the country's people"*, although Munich is 17 minutes away by train.
Jobcu had split the sentence into "the job's own town has 0.3%" and an unchecked travel time.

| Decision | Reason |
|---|---|
| **A travel time or distance TO a kind of place is one condition** ("near"): a limit (minutes and how the person travels, or km) and reference places (towns of a size, named towns, or kinds of places the AI looks up on the web, such as university towns). The AI reads each person's own wording; nothing about 50 minutes, 0.3% or public transport is built in. | The owner's example and instruction: every user may want something completely different, and the AI must think about what they mean. |
| *Superseded (2026-09-21 night): no company-address look-ups, see the next section.* **Where the job is:** the coordinates a job site gives, otherwise the centre of the job's town. With a Google Maps key, jobs within 10 minutes of the limit are measured again from the company's own address (Places text search). | The owner asked for the exact company location when possible, otherwise the best estimate. Looking up every company would use the free allowance quickly; only close calls need it. |
| *Superseded in part (2026-09-22): car trips are measured without live traffic, see "What the owner's first real search showed".* **Travel times come from Google Maps (Routes API, weekday 8:00 local time) with the person's own optional key**, otherwise from the AI as a clearly labelled estimate. Straight-line distances settle the clear cases without asking anyone: a job inside a reference town, and reference places no train or car could reach in time. Only the 3 nearest remaining places are asked about, only for jobs that passed the quick relevance check, and once per place, not per job. | The owner's choice of Google Maps (2026-09-17) and his wish to use it now. Cost: Google's free allowance is 10,000 route elements and 5,000 place look-ups a month (checked 2026-09-21); Jobcu stops at 9,000 and 4,500 (changeable) and falls back to AI estimates with a note. |
| **Each job's travel minutes are kept with the condition in the search's results**, so a corrected limit in Edit is applied at once without asking Google or the AI again; a different way of travelling measures again. Nothing is remembered between searches (the owner's open question about remembering place facts still stands). | Corrections must be cheap; "everything starts fresh each search" still holds. |
| **Google Maps is the only service besides job sources and the AI provider**, and only when the user saved a key (AGENTS.md hard rule 2 updated). | Recorded as the owner's decision so assistants don't treat it as a rule violation. |

## 2026-09-21 (night): The owner's answers on travel and on facts about places

| Decision | Reason |
|---|---|
| *Superseded (2026-09-22): Google's times may not be kept, see "Keeping the project recoverable and tidy".* **Travel times are remembered for 30 days** when the place is the same: the same town, or the same point the job ad gave, to the same reference place, by the same way of travelling (`travel_memory`). With a Google Maps key, only Google's answers are reused; AI estimates are reused only while there's no key. | The owner's answer to the open question. Timetables barely change in a month, and repeated searches then use almost none of the free allowance. |
| **No company-address look-ups** (this overrides the Places refinement from earlier the same evening). Jobs are placed where the job ad says: its exact point if the site gives one, otherwise its district or town. | The owner: a company can have several sites with the same name, so an address search can pick the wrong one. |
| **Any fact about places is analysed by the person's AI**, whatever it is (election results, sunshine, shops, rankings, laws). The answer may be towns that fit, towns to avoid, a size rule, or **countries that fit / to avoid**; country answers also narrow which countries are searched. When no source lists every place, the AI reasons to the most useful rule and labels it an estimate instead of giving up. | The owner: "that's why we have the built-in AI", with examples such as far-right results, sunshine, Turkish shops, a top-10 work-life-balance country. Checked live: work-life balance narrowed the search to 11 countries (OECD Better Life Index), sunshine gave northern towns to avoid, Turkish shops an estimated size rule. |
| **The same fact can decide both the job's own town and the places a person travels from.** The AI decides from the wording (both when unsure, and says so); the web is searched once for both. | The owner's own sentence: "no AfD cities" plus "50 minutes to a big city" should rule out Dresden as a place to travel from too. Checked live: the AI applied it to both with one look-up. |
| **Spending on AI and Google Maps is fine within the €30 a month** (lower is better, free is best). | The owner's reminder; it matches the budget in AGENTS.md. |

## 2026-09-22: Keeping the project recoverable and tidy

| Decision | Reason |
|---|---|
| **One home for each kind of information** (the table in AGENTS.md): AGENTS.md is the only rulebook, for people and every AI; PROGRESS.md only says where things stand; DECISIONS.md the reasons; SOURCES.md source facts; README and guides for users; git history for what changed. CONTRIBUTING.md is short: setup, and two ready-made prompts for any AI assistant (starting a session, and wrapping up before a fresh one). | The owner wants the project to survive any interruption and to be continued by anyone with any mainstream AI. Duplicated records drift apart; the "dominant election result" rule was lost that way. |
| **"Right now" has fixed sections:** State, In progress, Verify before relying on, Waiting on the owner, Next tasks, Known limitations. Multi-step work is written under In progress before it starts, so an interrupted task can be recovered. No diary: the long "Last finished" histories were removed. | Recovery after usage limits, compaction or crashes, with the least to read. `tests/test_docs.py` checks the sections exist. |
| **Replaced decisions are marked *Superseded*** in this log instead of relying on "newer rows win". | Readers, human or AI, shouldn't have to work out which of several rows still applies. |
| **AI tools' private memories hold no project knowledge.** The Claude-only memory notes were folded into AGENTS.md and PROGRESS.md and removed. | Other assistants can't see them, and they drifted from the repository. |
| **The owner's reading of party conditions is part of the AI's research instructions:** party labels mean the parties reliable sources put in that group; "dominant" or "strong" means above the party's national share at the latest national parliamentary election, unless the person's words say otherwise. | Decided on 2026-09-17 but never given to the AI, so Dresden stayed in. Checked live: now excluded, both as a job town and as a place to travel from. |
| **Google's travel times are not remembered between searches; the AI's estimates are (30 days).** | Google's Routes API terms (section 19.3) allow keeping only coordinates, for 30 days, not durations. The owner allowed 30 days "as far as Google's terms allow" (2026-09-17), so his 2026-09-21 answer applies to the estimates. |
| **Removed as unused:** `KeyStore.has`, `paths.code_dir`, and the unused "fake documents folder" exception in the safety check (tests build fake documents in code). On GitHub the empty wiki and project boards are switched off, and labels that duplicated others (bug, enhancement) or didn't fit a private project were removed. | The owner asked for a clean, sleek project with nothing that doesn't add value. |

## 2026-09-22: The owner's Google billing

| Decision | Reason |
|---|---|
| **The owner's Gemini use is paid, not free:** billing is on, with €15 paid in advance. Real tests on his key therefore cost a little money: keep them small, within his limits below. | Seen on his Budgets & alerts screen: €0.61 spent on the Gemini API this month. "No credits used" there only means no free promotional credit from Google. |
| **Never more than €25 a month for everything, for now.** The AI: Google's spend cap on the budget "Jobcu AI" pauses the Gemini API at **€23** (the pause can come a few minutes late, so €2 is left for that). Google Maps: stays inside the free allowance, so €0. Maps can't have a spend cap, so Google's own daily quota stops it: **320 route-matrix elements a day** (31 × 320 = 9,920, below the free 10,000 Essentials elements), **0 ComputeRoutes requests a day** (Jobcu never uses them), plus a €1 email alert on the budget "Jobcu Google Maps". Jobcu's own monthly stop (9,000) stays as well; when a stop is reached, travel times fall back to AI estimates. | The owner first asked for €20 and €1, then: "a little bit over 20€ is okay … but I definitely don't want to spend more than 25€ everything included". Jobcu's route-matrix requests (TRANSIT, DRIVE, WALK, BICYCLE, no traffic-aware routing) are Essentials, the SKU with 10,000 free elements. This also answers the 2026-09-17 request for a Google-side daily limit. |
| **One clearly named item per thing Jobcu uses, nothing else:** billing account "Jobcu"; projects "Jobcu AI" (ID gen-lang-client-0103514035) and "Jobcu Google Maps" (ID jobcu-509321; IDs can't be changed); keys "Jobcu AI key" (Gemini API only) and "Jobcu Google Maps key" (Routes API only); budgets "Jobcu AI" and "Jobcu Google Maps". The account-wide €20 alert was removed. | The owner wants it clean and without duplicates. Nothing else runs on his billing account; if that changes, add a budget for it. |
| **Google Maps' European (EEA) terms were accepted** for the "Jobcu Google Maps" project, with the owner's yes. | Required to turn on the Routes API with a European billing address. Jobcu complies: it shows travel minutes as text, never Google content on or next to a map. |
| **Only what Jobcu uses is switched on.** "Jobcu AI": the Gemini API only. "Jobcu Google Maps": the Routes API, plus Service Usage, Service Management, Logging, Monitoring and Telemetry, which the console itself needs; Google's other default services (BigQuery family, Dataform, Dataplex, Analytics Hub, Datastore, Cloud SQL, Cloud Storage, Trace, the "Google Cloud APIs" bundle) are off. The unused service account "Default Gemini API Key" was deleted. | The owner: delete everything Jobcu doesn't use. Every kind of location condition is covered by these two services: travel times by the Routes API; sizes and distances by the shipped town list; any other fact by the AI's web search, which is part of the Gemini API. A future feature that needs another Google service switches it on then, and adds it to the key. Checked after the cleanup: Jobcu's AI connection test and a web look-up both work. None of this touches the owner's personal Google services (Gmail, Drive, YouTube, the Gemini app, "Sign in with Google"), which don't run on his Cloud projects. |

## 2026-09-22 (evening): What the owner's first real search showed

| Decision | Reason |
|---|---|
| **When no job site says which town a job is in, the quick relevance check reads it from the ad text** (title and first ~700 characters), and the conditions about places and travel times then use it. A town is accepted only when the text really names it and it is in the town list; "from the ad text" is shown on the card. It is kept with the job (`JobGroup.place_from_text`), so Edit never asks again. | In search 5, 142 of 165 scored jobs came from Adzuna with only "Deutschland" or "UK": Adzuna's newer ads carry no town, their pages refuse Jobcu (403), but their text usually says "am Standort in …". The quick check already reads every such job, so this costs almost nothing extra. Real check: 36–42 of 101 related jobs got a town; the rest name none. |
| **Car trips are measured without a departure time, so without live traffic.** Public transport keeps the weekday 8:00 timetable. | Google refuses a departure time for car trips unless live traffic is asked for, and live traffic is its "Pro" tier: half the free allowance at twice the price, which the owner's €25 limit can't carry. Found in search 5 (every car trip answered 400). |
| **A place that is only a country, state or nation is never read as a town**, and **"Town-District" names count as the town** ("Wietmarschen-Lohne" is Wietmarschen, not Löhne). An avoid-list answers "unknown" for such places, not "fits". | "Sachsen" was read as the village Sachsen bei Ansbach and "Brandenburg" as Brandenburg an der Havel, which could rule jobs out wrongly under a size condition. |
| **When a job's town is unknown, its card says so once** ("The ad doesn't say which town the job is in, so your conditions about places couldn't be checked") instead of one "couldn't be checked" per condition. Such jobs stay in the results. | Clearer cards; not missing jobs comes before filtering them. |
| **A fact that two conditions use is looked up once, in the fuller wording**, even when the travel condition names it in fewer words. | The owner's sentence put the fact in brackets; the travel condition's reference places were researched from the bracket alone, which the AI read as turnout, and came back with far-right strongholds. |
| **Research lists the minority side, completely:** when a condition holds for most places ("below the national average"), it lists the towns where it fails, as towns to avoid, small towns included; the research step has room for up to 400 towns (16,000 tokens, paid only as used). If the fact can't be checked, a travel limit keeps its size or named places. | The first reading gave 31 "towns that fit", so Munich, Stuttgart, Ulm, Kassel and Hannover jobs failed. With the change, the owner's sentence gave 71 towns to avoid (Dresden, Erfurt, Gelsenkirchen, Clacton-on-Sea…), Munich, Stuttgart, Leipzig, London, Dublin fit, and one look-up served both conditions (5 web searches). |
| **The quick relevance check's wording stays as it was for now.** | In the real check it called 42 of 147 previously kept jobs unrelated (full scores 38–71, e.g. "KiCad Developer" 68) when they were batched together, but only 19 in mixed batches; the old wording did the same, and a more careful wording barely changed it (39). It needs the owner's ratings to tune (next tasks). |
| **Search words for skills must be specific to the person's kind of work**; no general activities (commissioning, installation, maintenance…) or everyday objects of other trades ("inverter design", not "inverter"). | Sources match skill words anywhere in an ad. In search 6, "Inbetriebnahme" and "Wechselrichter" alone made Adzuna's German query match 6,459 ads in three days instead of 337. |
| **Adzuna asks the precise way first:** each job title in the ad's title (`title_only`; a title containing another one's words is skipped, as that one finds it), then specific single words together, then phrases, all by relevance within the window. Each search gets a fair part of what is left of its country's share, and what a country doesn't need goes to the next. | Search 6: 3,000 Adzuna ads, 85% unrelated, and the budget was spent on the newest noise before the precise searches ran; the quick check then paid to read them all. The new plan, run for real: 192 ads from 19 requests, 78% related. A broad search now reads its most relevant ads instead of the newest. |
| **A card whose town is unknown names the job site** ("Adzuna doesn't say which town this job is in, nor does its text…"). | Honest about where the gap is: the job site, not the employer. |

## 2026-09-22 (night): Finding where the best jobs are

| Decision | Reason |
|---|---|
| **After scoring, the person's AI finds the town of the best jobs still without one online** (`jobplace.py`): score 50 or more, best first, five per request, within the search's web look-ups. The instructions require a real search for the exact ad and forbid answers from memory or the company's head office; a town counts only if the town list knows it in the job's country. Conditions and travel times are then applied; failures go to "Left out by your conditions"; cards say "(found online)"; Edit never asks again. | The owner: the job's location is a really important parameter, and the built-in AI is there to find it. Jobs without a town stay mixed in by score (his choice). Real check on search 6: 5 of the 10 best town-less jobs found (TRONTEQ Reutlingen, LITEF Freiburg, Tesla Automation Prüm, IHSE Oberteuringen, Bolton), the rest "unknown" rather than guessed; about 3 cents and 5 free web searches. Without "search for every job" in the instructions the model answered from memory (0 searches). |
| **Not built: matching town-less jobs to the Bundesagentur's ads.** | Its employer filter returns nothing; searching by title and matching the employer found 5 of 42 reliably, with agencies (the same title in 24 towns) and false matches on shared words. |
| **Web look-ups per search: 50 by default, shown in Settings** ("Web look-ups by your AI in one search"); the owner's is 50. **Scoring limit: 200 by default**; the owner's is 200. | Understanding the location takes about 10 look-ups; the rest serves finding towns. Gemini gives 5,000 free search queries a month, enough for several searches a day. The owner clicked "Score more" in every search at 150; with precise sources 200 rarely asks. |


## 2026-09-23: What the owner's second real search showed

| Decision | Reason |
|---|---|
| **Travel times ask about the 2 nearest reference places, not 3**, and the owner's Google daily quota is 450 elements. A "quota exceeded … per day" answer now says "Google Maps' daily limit is used up. It resets at midnight in California (about 9 in the morning in central Europe)." | Search 7 fell back to AI estimates because 297 elements on 22 September and 37 more at 02:00 on 23 September shared one Google day (Pacific). The nearest place by straight line is almost always the fastest, so two keep the answer with half the use. |
| **One failing Adzuna query leaves the source partial; the search goes on with the other search words.** Three failures in a row still fail the source. | Adzuna answered 503 in search 7 and Jobcu dropped the whole source after 11 requests, collecting 663 ads instead of a full round. |
| **A note is shown once per search**, however many steps report it. | "Google Maps asked Jobcu to slow down" appeared twice, once per measuring round. |
| **Every scored card shows how the score adds up** (Role & skills 38/40 · Seniority 20/20 · …), the five rubric parts from `scoring.py`; a test keeps the screen and the rubric in step. | The owner: "I didn't get the 1 point difference between postings." The breakdown existed only as a hover tooltip, which nobody finds. |
| **A job left out by a condition shows only what left it out**, not "couldn't be checked" lines about the other conditions. | The owner asked why left-out jobs said Jobcu couldn't decide: those lines were about conditions that never got to decide, which reads as a fault. |
| **Looking a job up online asks for the place of the work**, never a staffing agency's own office. | Search 7 found towns for 18 of 20, several of them agency ads (FERCHAU, Hays, Brunel), where the agency's branch is the wrong answer. |

## 2026-09-23 (evening): What the owner's third search showed

| Decision | Reason |
|---|---|
| **The owner's limits for clear blockers** (his answers, 2026-09-23): a must-have language **two or more levels** above the person's (A2 against B2): languages 0 of 15 and the total **at most 65**; one level short 8 of 15, only "a plus" 12 of 15, no limit. "Good" and "very good" count as **B2**, "fluent" and "verhandlungssicher" as C1, a language asked without a level as B2. An ad **written in** a language the person speaks below B2 that says nothing about it: **low but not 0, no limit** (5 of 15 two levels short, 10 one level short, 3 further). A citizenship or security clearance the person **definitely** can't get, which the ad **definitely** asks: **at most 30**. A required doctorate the person lacks: **at most 50**. Experience: **5+ years** more than the person's full-time years: **at most 75**; **8+**: **at most 60**. The card shows "Limited to 65 (the parts add up to 81): German B2 required, you have A2". | Search 8: 15 of the 24 jobs scoring 85+ were German-language ads, and 29 of the 32 full German ads asked for good or very good German, yet scored 80–90 with 5 of 15 for languages. A UK-nationals-only Rolls-Royce job scored 80 and a 5+ years senior role 86. HANDOVER §11 keeps: low scores rank lower but are never hidden. Real check, search 8's 242 jobs scored again (Gemini, 4 minutes, Adzuna still from summaries): 85+ fell from 29 to 14 and 70+ from 126 to 81; all 31 German ads asking "gute", "sehr gute" or "fließende" German were limited to 65; MBDA and Rolls-Royce "British or dual UK national" jobs to 30; 17 jobs asking 5–7 years to 75 and 5 asking 8–15 years to 60. |
| **The AI reads the evidence first, code decides the language part and the limits.** Per job the AI gives the ad's language, the languages asked (CEFR level, must-have or a plus, the ad's words), the years required, whether a doctorate is required and the person lacks it, and whether a citizenship or clearance is definitely out of reach; `scoring.py` turns this into points and limits. The other four parts stay the AI's judgement, with the rubric weights unchanged. | The owner's rules are exact, so they belong in code: the same evidence always gives the same score, and tests hold each rule. Asking for the evidence before the points also makes the model read the requirements before judging. |
| **Unknown isn't treated as fine.** Requirements get 15 only when the whole ad was read; 11–14 when only its start is known. Location & wishes get 9–10 only when the person's stated preferences are met, 3–5 when the job conflicts with one (a contract for someone wanting full-time work). | In search 8, English jobs scored from a 500-character summary averaged 73, from the full ad 66: missing requirements counted as met. |
| **"Anything your documents don't say" (optional)** on the search screen, read with the documents into the profile: citizenship, work permits, a newer language level. The note overrides the documents where they differ. Without it, the citizenship limit never applies. | The owner: a person's citizenship decides which clearances they can get, and "this may change for each person". HANDOVER §4 allows citizenship only when the person states it; this gives everyone a place to state it without editing their CV. |
| **A language the documents are written in but don't list is added to the profile**, at the level the writing shows. | Otherwise a German native with a German CV and no languages section would be treated as not speaking German by the "written in" rule. |
| *Superseded (2026-09-24): Adzuna's pages aren't read.* **Adzuna's town-less ads are read through `/details/<id>`**, and CloudFront's "Request blocked" page counts as a block: page reading stops at the first one. | The `/land/ad/` links always refuse; three of them in a row had stopped all page reading since search 4 (search 8: 0 of 164 Adzuna ads read in full). Half an hour after three test pages loaded, Adzuna's firewall refused Jobcu's page requests altogether, so Jobcu backs off at once (AGENTS.md hard rule 1). |
| *Superseded in part (2026-09-24): from 50, see "What to do with Adzuna".* **Summaries near the top are read online.** After scoring, jobs scored from a short summary with 70 or more are looked up with the person's AI together with the town-less ones (`jobplace.py`, one request per five jobs, within the search's web look-ups); the full ad's languages and years replace what the summary suggested, and the rules above are applied again (`scoring.with_ad_read_online`). The evidence is kept with each score for this; the card says "Languages and experience read from the full ad online". Full ads Jobcu read itself are never overridden. | Re-scored search 8: 68 German ads Jobcu could only read as a summary stayed at up to 85 under the "written in German, no level" rule, while 29 of 32 German full ads asked for B2 or more. Looking them up is real evidence rather than a guess, and it can also raise a job whose full ad says English is enough. Real check on 10 of them: all 10 ads found online, 8 ask for German B2 or C1 ("Deutschkenntnisse auf mindestens C1 Niveau", GEZ Rail; "sicher auf Deutsch und Englisch", Terepco). |
| *Superseded in part (2026-09-24): the look-up thinks at the person's reasoning setting, medium by default, see "Medium reasoning everywhere".* **The online look-up searches freely, then a second step structures its notes**, with **low thinking**, and **an answer given without any web search is asked again once, then ignored**. The level and years rules are shared with scoring (`scoring.LEVEL_RULES`, `YEARS_RULES`). | With a strict one-line answer format Gemini searched for none of 10 jobs and answered from memory ("German B2 must" for every German ad, no sources). Written freely it made 6–11 searches per five jobs and quoted the ads. Low thinking found the same with about 1,200 output tokens per five jobs instead of about 5,100, so the look-up now costs less than the old town-only one (search 8: 27k output tokens for 53 jobs). |
| **The ad text decides the job type when the job site leaves it open** (several types, such as Adzuna's "contract": fixed-term, freelance or part-time). A job whose ad then turns out to be a type not ticked is left out as "A job type you didn't tick, according to the ad text". One clear type from a site still wins. | Search 8 showed a UK "contract outside IR35" job at 83 to someone who ticked only full-time and fixed-term. |
| **The scoring evidence no longer asks for the ad's words about each language.** | They weren't shown anywhere, and the new evidence had raised scoring output from 37k to 53k tokens in search 8 (about €0.04 a search). |
| **Facts can be decided for whole regions.** The research may answer "all of Saxony, except Jena", for states, provinces and nations (GeoNames level 1) and counties or districts (level 2; in Germany the Kreise, level 3, because its level 2 is the Regierungsbezirk). Every town in the shipped list carries its region and district (`places.csv.gz`), and `regions.csv.gz` holds 8,138 region names in English and the local languages. Conditions, reference places and the Edit window use them; in the Edit window a whole region is written "All of Saxony" and an exception "Except Leipzig". | Search 8: the AI's list of 60 German towns missed Dresden and Radeberg (Landkreis Bautzen), so both passed a condition that should rule out all of Saxony, and Dresden became a reference place. No list of up to 400 towns holds every small town in the east. Real check with the owner's sentence: Saxony, Thuringia, Saxony-Anhalt, Brandenburg, Mecklenburg-Vorpommern and Saarland avoided except Jena and Potsdam, plus 43 towns in the west and the UK; Radeberg and Dresden left out, and neither Dresden nor Chemnitz measured to. |
| **Towns to avoid that name nothing in a country leave all of that country fine.** | Ireland came back with no town to avoid, which read as "couldn't be checked" for every Irish job. |

## 2026-09-24: Medium reasoning everywhere

| Decision | Reason |
|---|---|
| **Every AI step uses medium reasoning** (the owner: "we should use medium reasoning for every gemini thing"): the default for the small steps (quick check, scoring) is now medium like the reasoning steps, web look-ups use the reasoning setting instead of the provider's default, and settings files from before (version 1, where "low" was a default nobody could change on screen) move to medium once. It holds for every provider (AGENTS.md rule 4). | Measured on search 8's jobs with the owner's Gemini: scoring +115 thinking tokens a job, scores moved 3.5 points on average (an "R&D Hardware Project Manager" 67 → 50); the quick check the same verdict with +900 tokens per 40 titles; the online look-up 15 web searches for five jobs instead of 6–9, +2,200 tokens per five jobs. A search like search 8 comes to about €0.45 instead of €0.30, so the €23 AI budget holds about 50 searches a month. Whether medium scores better is for the score check (HANDOVER §13). |
| **Normal use is one 24-hour search a day**; 72-hour searches are for development only. The AI cost of medium reasoning is fine, and the budget stays: €23 for the AI, €25 for everything. | The owner: "why someone would need to search last 72h every day". A 24-hour search finds about a third of a 72-hour one's jobs, so it costs roughly a third of search 8's €0.45. |
| **A reference place is where the person would live** (the owner, 2026-09-24): a job whose own town is an eligible reference place is in it, wherever in the town the ad's address is, with no trip to measure and no Google request (`travel.home_town`); conditions about the kind of place belong to the reference places too, and to the job's own town unless the person's words limit them to where they live. Written as a general rule in the AI's instructions, never as the owner's case. | Search 8 measured "Moosach, München" (an address 7 km from Munich's centre) as a 27-minute drive to Munich; with a 20-minute limit that job would have been left out. The owner: "if the job is itself in Augsburg, then it shouldn't search for the closest city"; "the search cities also should be eligible for the far-right election criteria (or other criteria)"; "each [user] will use something completely different than mine". |
| **Every AI request gets room for thinking on top of its answer** (medium: 12,000 tokens, low: 4,000, high: 16,000, at most 32,000 in all); only what is used is paid for. | Thinking counts towards the output limit with every provider. After the switch to medium, the step that sorts location conditions (2,000 tokens) was cut off, and Jobcu fell back to looking every condition up on the web: "at most 45 minutes by train from a city with a technical university" became a guessed list of 104 towns instead of a measured travel limit. |
| **Every travel time or distance is a travel limit ("near"), also when its places must be looked up**, and **the research and its structuring decide "fit" or "avoid" from the person's wish, not from the fact**; towns are written as towns, never constituencies. | Real checks with a second, made-up person's sentence ("Netherlands or Belgium, at most 45 minutes by train from a city with a technical university, and not somewhere with a lot of rain"): once the rainy towns came back as towns that fit, which would have kept only rainy places. After the change, four different travel conditions were read as travel limits twice over, the rain as towns to avoid. UK answers had named "Boston and Skegness" and "South Basildon and East Thurrock", which match no town. |

## 2026-09-24: What to do with Adzuna

| Decision | Reason |
|---|---|
| **Adzuna stays: it is the biggest source.** In search 8 it gave 351 of 774 ads, and 136 of the 242 jobs worth scoring came from Adzuna alone, more than all other sources together. | The owner felt Adzuna "is causing problems all the time" and left the decision to Claude. Coverage comes before tidiness (AGENTS.md: the order of goals); the problems are fixed where they arise instead. |
| **Jobcu doesn't read Adzuna's own job pages any more.** Its jobs are scored from the ~500 characters the API gives, and the full ad comes from the same job on another site or from the person's AI reading it online. | Its firewall has refused Jobcu's page requests since 2026-09-23 evening, and its robots.txt answers 403 to Jobcu, so its rules for pages can't even be read (AGENTS.md hard rule 1). Before that, a bug had stopped all page reading since search 4: 0 of 164 Adzuna ads in search 8 were read in full. Trying each search only brought a refusal and a note. |
| **A place that is only a country or a region ("Deutschland", "Sachsen", "UK") never stops two ads of the same company and title from being the same job.** | "Deutschland" had counted as a town, so Adzuna's town-less ads never merged with the same job elsewhere. Search 8 regrouped: 631 jobs became 602 (29 duplicate cards gone, mostly Adzuna's own reposts), and KLA and Sennheiser got their town and full ad from Workday and the Bundesagentur. |
| **Every summary-only job scoring 50 or more is read online** (was 70), best first, within the search's web look-ups. | With a 24-hour search a day there are about a third as many jobs, and summaries are now the rule for Adzuna: evidence rather than a summary's guess for every job that could make the list. |
| **When the search's web look-ups run out, Jobcu asks** whether to read the remaining jobs online too ("Look up 12 more" / "Show results now"), like the scoring limit; a yes gives the search another allowance. | HANDOVER §13: caps never silently reduce coverage. With every summary scoring 50 or more read online, the default of 50 look-ups can run out, and the look-up used to stop without a word. |
| **Adzuna's monthly allowance is shared by how often this person really searches** (the last 14 days, at least one a day), between 25 and 60 requests a search and never more than is left today. | It assumed 3 searches a day; the owner runs one 24-hour search a day, and others may run more. Measured use gives a once-a-day searcher up to 60 requests (more jobs) and still spreads a heavy user's month. |

## 2026-09-24 (later): Cloud sessions, a universal Jobcu, and the order of countries

| Decision | Reason |
|---|---|
| **Development moves to Claude Code cloud sessions** (claude.ai/code, Opus 5.5 with Extra effort, the owner's $100 cloud credit until 2026-11-05); Jobcu itself stays on the owner's Mac. The repository prepares each session (`.claude/settings.json` runs `tools/cloud_setup.sh`); the environment needs **Full** network access. The owner's CV, data folder and keys never go to the cloud; real-AI checks there only through an API credential on the environment, which no session can read. Tasks that need his data run on his Mac. | The owner's choice. Cloud sessions keep working when his laptop is closed, and the credit pays for long, careful work. Hard rule 2 is about Jobcu itself, not the tools used to build it, but his personal data follows the same spirit. |
| **Pull requests are merged with a merge commit, never squashed or rebased.** In a cloud session the agent merges its own pull request when GitHub's tests pass, if the owner allowed it in that session. GitHub enforces it (the owner's yes, 2026-09-24): only merge commits are allowed, and merged branches are deleted automatically (their commits stay in main). | Cloud sessions work on their own branches. Squashing would replace a session's commits with one, losing the dated record of the owner's work (hard rule 6). |
| **Jobcu is universal**: any profession, any of the 30 countries, any wording. Nothing is tuned to the owner's profile; prompts use examples from several professions, and changes are tried with made-up people from other fields. | The owner: "maybe he will not look for technical jobs… teaching jobs, anything else… don't build it optimised for me. That's why we are using a built-in AI." Today's prompts and the employer directory still lean to engineering (PROGRESS.md, next tasks). |
| **The order of countries** for development and testing: 1. Germany; 2. the UK and Ireland; 3. Switzerland; 4. Belgium; 5. the Netherlands; 6. Italy; 7. Scandinavia (Denmark, Norway, Sweden); 8. Poland; the rest later. | The owner's order. *Supersedes the 2026-09-17 (late evening) order "Ireland, the UK and Germany first".* |
| **LinkedIn and StepStone are reached only by legitimate routes:** the same jobs on employers' own career sites and national job services, aggregators with an API, the person's own AI searching the web (it reads what its search engine returns; Jobcu never opens their pages itself), or their permission. No logins, no scraping of their pages or hidden APIs. | The owner asked for the best way to match the two biggest platforms without being banned. Both forbid automated access in their terms (hard rule 1); the next sessions research the routes above per country (PROGRESS.md). |
| **Adzuna's pages stay unread, and the question stays open only in one direction:** if Adzuna's robots.txt becomes readable and allows job pages, and its firewall stops refusing Jobcu, reading them may come back, with the owner's yes. | The owner asked whether this hurts search quality. It doesn't change what he has had: no Adzuna ad was read in full in search 8 anyway. Coverage is unchanged (the jobs come from Adzuna's API), and every Adzuna job that could make the list is now read online at its source. What it costs: jobs scoring under 50 keep their summary's evidence, and reading online costs a little AI. |
| **The cloud work is paid only by the $100 credit, and ends with a final handover** to local sessions: the owner watches the balance and sends the final-handover prompt at about $85 used; until then every finished step is merged at once, one session runs at a time, and the context is kept lean (AGENTS.md, "Budget and the final handover"). | The owner: "I definitely don't want to spend more than the $100… after the free credits finish I will come back to Claude Code… it should be protected for possible interruptions." A session can't see the credit left (the balance shows only at claude.ai, Settings → Usage), so no session can stop itself at $85; merging every step makes an abrupt stop cheap. |
| **Money: at most €25 a month for the owner, everything included; free, or under €10 a month, for everyone else.** Cheaper is better and free is best; while Jobcu is being developed the owner is more relaxed within his €25. Jobcu must work well on free allowances and cheap models, and the defaults are chosen with that in mind. | The owner (2026-09-24): "my friends and other people will probably not even want to spend 10€ and try to stay as free as possible." *Supersedes the €30 of 2026-09-17 (evening), already lowered to €25 for the owner on 2026-09-22.* |
| **The development loop:** cloud sessions build and test with made-up people (fake CVs in code, the Gemini API credential, the sources that need no key); the owner runs a **72-hour search every two or three days** on his Mac while Jobcu is being developed (one 24-hour search a day once he uses it for real), and a short **local** session studies it with the prompt "Check my latest search" (CONTRIBUTING.md), recording findings in PROGRESS.md for the cloud sessions. **The launchers update a git copy of Jobcu from GitHub before starting** (only on main, only with nothing changed locally, never asking for a password; ZIP copies skip it). | The owner's CV, keys and history stay on his Mac, so only his own searches show how Jobcu works for him; a 72-hour search gives three times as many jobs to find faults in, for about €0.45. Local sessions use his subscription, not the cloud credit. Without the update step, his Mac would keep running old code while the cloud sessions improve it. |


## 2026-09-24 (evening): What the research on sources and job platforms showed

Next task 1 (PROGRESS.md). Source facts are in SOURCES.md, "Candidate sources by country".

| Decision | Reason |
|---|---|
| **Each country's public employment service and public-sector job portals come first**, through their open data, feeds and APIs (service.bund.de's feed, NHS Jobs, Teaching Vacancies, Le Forem's open data, NAV's feed, Werken voor Nederland), then career systems common in that country (d.vinci, prospective.ch), then commercial routes. | They carry every kind of work (teachers, nurses, clerks, trades, housekeeping), most allow reuse openly (Open Government Licence, CC BY-SA, "everyone can use the service"), and they add jobs the commercial routes lack: 17 of 25 service.bund.de jobs weren't on the Bundesagentur. Schools, health services and councils rarely post on commercial boards, so without them Jobcu can't be universal. |
| **Jobcu shows when a job is a repost.** A job whose ad says "posted today" but which Jobcu's job memory first saw in an earlier search gets "First seen by Jobcu on 14 August" on its card; where a source gives a first-publication date (the Bundesagentur), that date is used, as now. | About one job ad in five is never filled (Greenhouse, 2025; 27% of US LinkedIn listings in another analysis), and reposting an old ad makes it look fresh. Google's rules forbid resetting `datePosted`, but not every site follows them. Jobcu already remembers each job's first search (`jobstore`), so this costs nothing. |
| **Closing dates are kept and shown** ("Apply by 8 October") when a source gives them (NHS Jobs `closeDate`, service.bund.de's Bewerbungsfrist, Teaching Vacancies' `validThrough`, Le Forem's end date, prospective.ch's `end_date`), and a job whose closing date has passed is left out as a fixed rule. | Public-sector and school jobs close on a fixed day; a person needs to see how long is left. A passed deadline is an objective fact, like a proven-old posting date (HANDOVER §11, rules pre-filter). |
| **ESCO, the EU's open occupation classification, is a ruler for the search words, never a filter.** The universality audit (next task 2) checks the AI's search words for made-up people against ESCO's names for their occupation in each language (for example "Gesundheits- und Krankenpfleger" and "Pflegefachkraft" for a nurse). | Keyword sources (Adzuna, Reed, the Bundesagentur) find only what the search words name, and job titles vary most outside engineering. ESCO is free, needs no key (`ec.europa.eu/esco/api`) and covers every EU language; the person's AI still decides the words. |
| **Leading platforms' matching, and what Jobcu takes from it:** LinkedIn's Job Match (2025) lists the qualifications a person meets and lacks; Google for Jobs relies on each site's JobPosting data and drops expired jobs; public services classify ads by occupation codes (NAV's ESCO and STYRK codes, Le Forem's occupation codes, Teaching Vacancies' categories). Jobcu keeps its own AI reading of the whole ad and the owner's rubric; a proposal to show met and missing must-haves and to limit scores for a missing required licence waits on the owner (PROGRESS.md). | Jobcu's advantage is that the person's own AI reads each ad against their CV, which no fixed taxonomy does for every profession. What the platforms do better is show the gaps plainly, which the card could do from evidence the scoring already collects. |
| **The first public sources are built before the universality audit** (PROGRESS.md, next tasks): service.bund.de, Teaching Vacancies, NHS Jobs, then closing dates and reposts. | Coverage comes before scoring (AGENTS.md, the order of goals), and auditing a teacher's or a nurse's search means little while Jobcu has no source for schools or hospitals. |
| **Jobs the person's AI finds on the web are shown once Jobcu confirms them** at a source it may read (the job's own page where allowed, the employer, a permitted board or service); whether to also show unconfirmed ones from LinkedIn, StepStone or Indeed, marked as such, is the owner's question (PROGRESS.md). | A live test with four made-up people found real, fresh jobs, but with made-up or list-page links and mixed-up IDs (SOURCES.md). HANDOVER §9.6: web search is for finding jobs, not for trusting details. |

## 2026-09-24 (evening): Building the public sources

| Decision | Reason |
|---|---|
| **service.bund.de is a source** (`servicebund.py`), for Germany: its RSS feed of the newest 500 public-sector jobs is read in one request and matched on Jobcu's side; job pages give the full ad, working hours, contract length, pay grade, coordinates and the employer's own application link. **At most 12 full ads a search**, 30 seconds apart; the rest keep their summary and are read online when they score 50 or more. When the feed doesn't reach back to the start of the window, the source says so ("partial"). | About two thirds of its jobs aren't on the Bundesagentur (SOURCES.md). robots.txt asks for 30 seconds between requests: twelve pages take six minutes, which a search can afford; more would make searches slow for every public-sector job seeker. |
